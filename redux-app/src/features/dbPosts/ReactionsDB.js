import React from 'react'

const ReactionsDB = ({postId}) => {
  return (
    <div style={{display:'flex'}}>
       <button>Like</button>
       <button>comment</button>
       <button>share</button>
    </div>
  )
}

export default ReactionsDB